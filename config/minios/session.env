# MiniOS session flags
# 1 = enabled, 0 = disabled, auto = context-based behavior

# In auto mode, compositor is disabled in virtualized environments.
MINIOS_ENABLE_COMPOSITOR=auto

# Restart panel/dock/compositor/tray when they crash.
MINIOS_ENABLE_WATCHDOG=1
